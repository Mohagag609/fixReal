name: Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.9
      uses: actions/setup-python@v4
      with:
        python-version: 3.9
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run tests
      run: |
        python manage.py test
    
    - name: Run linting
      run: |
        black --check .
        isort --check-only .
        flake8 .
        bandit -r .
    
    - name: Run security check
      run: |
        safety check
    
    - name: Collect static files
      run: |
        python manage.py collectstatic --noinput
    
    - name: Build Docker image
      run: |
        docker build -t makka:latest .
    
    - name: Test Docker image
      run: |
        docker run --rm makka:latest python manage.py check
    
    - name: Push to registry
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        echo "Pushing to registry"
        # Add push commands here

  build-staging:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop'
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.9
      uses: actions/setup-python@v4
      with:
        python-version: 3.9
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run tests
      run: |
        python manage.py test
    
    - name: Collect static files
      run: |
        python manage.py collectstatic --noinput
    
    - name: Build Docker image
      run: |
        docker build -t makka:staging .
    
    - name: Test Docker image
      run: |
        docker run --rm makka:staging python manage.py check
    
    - name: Push to staging registry
      run: |
        echo "Pushing to staging registry"
        # Add push commands here

  build-production:
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.9
      uses: actions/setup-python@v4
      with:
        python-version: 3.9
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run tests
      run: |
        python manage.py test
    
    - name: Run linting
      run: |
        black --check .
        isort --check-only .
        flake8 .
        bandit -r .
    
    - name: Run security check
      run: |
        safety check
    
    - name: Collect static files
      run: |
        python manage.py collectstatic --noinput
    
    - name: Build Docker image
      run: |
        docker build -t makka:${{ github.event.release.tag_name }} .
        docker build -t makka:latest .
    
    - name: Test Docker image
      run: |
        docker run --rm makka:${{ github.event.release.tag_name }} python manage.py check
    
    - name: Push to production registry
      run: |
        echo "Pushing to production registry"
        # Add push commands here

  build-docs:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.9
      uses: actions/setup-python@v4
      with:
        python-version: 3.9
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-dev.txt
    
    - name: Build documentation
      run: |
        sphinx-build -b html docs/ docs/_build/html
    
    - name: Deploy documentation
      if: github.ref == 'refs/heads/main'
      run: |
        echo "Deploying documentation"
        # Add deployment commands here